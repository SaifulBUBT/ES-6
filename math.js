
export const addi = (a,b) => a+b;

export const sub = (a,b) => a-b;

export const multy = (a,b) => a*b;

export const div = (a,b) => a/b;

export const mod = (a,b) => a%b;

const add2 = (x,y) => x+y;

console.log(add2(3,7));

export const car ={
    c_name: 'BMW',
    c_color: 'black',
    c_price: '$300000',

    // c_print(){
    //     console.log(`car name :${c_name}; Price: ${c_price}`);
    // }
}
